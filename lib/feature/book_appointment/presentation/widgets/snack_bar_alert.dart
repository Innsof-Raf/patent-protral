import 'package:flutter/material.dart';

class SnackBarAlert extends SnackBar {
  const SnackBarAlert({required super.content, super.key});

  Widget build(BuildContext context) {
    return Container();
  }
}
